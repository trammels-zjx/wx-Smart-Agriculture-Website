<form bindsubmit="formSubmit">
  <van-cell-group>
    <van-field
      value="{{ username }}"
      name="name"
      required
      clearable
      label="姓名"
      placeholder="请输入姓名"
    />

    <van-field
      value="{{ username }}"
      name="tel"
      required
      clearable
      label="手机号"
      placeholder="请输入手机号"
    />
      
  </van-cell-group>


  <textarea  placeholder="请输入申请商家原因"  name="textarea"/>
  <button class='btn' form-type="submit"> 提交 </button>

</form>